const AUTH = '/api/auth';
const LOGIN = '/';
const CREAR = '/create';
const VAL_TOKEN = '/newtoken';

const CUSTOMER = '/api/customer';
const CREAR_CUSTOMER = '/create';

module.exports = {
    AUTH,
    LOGIN,
    CREAR,
    VAL_TOKEN,
    CUSTOMER,
    CREAR_CUSTOMER
};